/target
*.proptest-regressions

# web/ - node deps, build output, and the wasm-pack output regenerated by
# `npm run dev`/`build` from crates/engine-wasm (see web/package.json)
web/node_modules/
web/dist/
web/src/wasm/
web/tsconfig.tsbuildinfo
